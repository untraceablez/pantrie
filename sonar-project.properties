# SonarQube analysis configuration for Pantrie.
# Override sonar.host.url / sonar.token via Jenkins (withSonarQubeEnv) or CLI.

sonar.projectKey=pantrie
sonar.projectName=Pantrie
sonar.sourceEncoding=UTF-8

# Production sources (backend Python + frontend TS/React)
sonar.sources=backend/src,frontend/src

# Tests
sonar.tests=backend/tests
sonar.test.inclusions=frontend/src/**/*.test.ts,frontend/src/**/*.test.tsx

# Coverage reports (produced by the test stages)
sonar.python.version=3.11
sonar.python.coverage.reportPaths=backend/coverage.xml
sonar.javascript.lcov.reportPaths=frontend/coverage/lcov.info
sonar.typescript.lcov.reportPaths=frontend/coverage/lcov.info

# Exclude generated / vendored / non-source paths from analysis and coverage
sonar.exclusions=backend/alembic/**,**/__pycache__/**,frontend/dist/**,frontend/coverage/**,**/node_modules/**,**/*.config.ts,**/*.config.js
sonar.coverage.exclusions=backend/tests/**,frontend/src/**/*.test.ts,frontend/src/**/*.test.tsx,frontend/src/main.tsx,frontend/src/test/**
